import gsap from "gsap";
import ScrollTrigger from "gsap-trial/ScrollTrigger";

gsap.registerPlugin({ ScrollTrigger })

const options = {
  wrapperRegisterAttribute: '[data-gsap-parallax-image]'
}

function registerGsapParallaxImage() {
}